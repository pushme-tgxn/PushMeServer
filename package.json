{
  "name": "pushme-server",
  "version": "1.6.0",
  "description": "Push Service backend",
  "main": "server.js",
  "scripts": {
    "start": "nodemon --trace-warnings server.js",
    "drop": "sequelize-cli db:drop",
    "heroku-postbuild": "npx sequelize-cli db:migrate",
    "migrate": "npx sequelize-cli db:migrate"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "expo-server-sdk": "^3.6.0",
    "express": "^4.17.1",
    "express-jwt": "^6.0.0",
    "googleapis": "^89.0.0",
    "mariadb": "^2.5.5",
    "sequelize": "^6.9.0",
    "uuid": "^8.3.2"
  },
  "engines": {
    "node": "16.x"
  },
  "license": "MIT",
  "devDependencies": {
    "nodemon": "^2.0.14",
    "sqlite3": "^5.0.2",
    "sequelize-cli": "^6.3.0"
  }
}
